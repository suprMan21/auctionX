# Dependencies
node_modules/
functions/node_modules/

# Firebase
.firebase/
functions/lib/
functions/.runtimeconfig.json

# Environment / secrets
.env
.env.*
*.local
.env.test

# Logs
npm-debug.log*
yarn-debug.log*
firebase-debug.log

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*.log

# Build output
dist/
build/
# Local emulator logs
firestore-debug.log
firestore-debug.log.*

.env.local

# Editor/backup junk
*.bak

.env.source

# 1Password account shorthand (each dev sets their own)
.op-account

# Allow .env.op template files (safe: contain op:// references, not real secrets)
!.env.op
!.env.local.op

# Supabase CLI cache leaked into frontend dir (auto-generated)
frontend/supabase/

# Supabase CLI remote-version cache (auto-generated, churns on every CLI call)
supabase/.temp/

# Claude Code worktrees + transient state
.claude/worktrees/