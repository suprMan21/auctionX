{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -z \"$SUPABASE_URL\" ]; then echo 'WARNING: SUPABASE_URL not set. Launch Claude Code with the cc-auctionx alias (op run wrapper) or run scripts/inject-secrets.sh first.' >&2; fi"
          }
        ]
      }
    ]
  }
}
